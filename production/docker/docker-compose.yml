services:
  cashbox-api:
    container_name: cashbox-manager
    build:
      context: ../..
      dockerfile: production/docker/Dockerfile
    ports:
      - "3633:3633"
    environment:
      NODE_ENV: production
      PORT: "3633"
    volumes:
      # Persist LevelDB outside the container (maps to app default data/config-db)
      - ../data/leveldb:/app/data/config-db
    restart: unless-stopped

  cashbox-ui:
    container_name: cashbox-ui
    build:
      context: .
      dockerfile: Dockerfile.remote-admin
      args:
        # Optional: pin a branch or tag (leave unset to use the repo default branch)
        # REMOTE_ADMIN_REF: master
        REMOTE_ADMIN_REPO: https://github.com/Permissionless-Software-Foundation/remote-admin.git
    ports:
      - "8080:80"
    depends_on:
      - cashbox-api
    restart: unless-stopped
